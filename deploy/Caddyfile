{$DOMAIN} {
    encode zstd gzip
    reverse_proxy social:8080

    header {
        X-Content-Type-Options nosniff
        X-Frame-Options DENY
        Referrer-Policy no-referrer
        -Server
    }
}
