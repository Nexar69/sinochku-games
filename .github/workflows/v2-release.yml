name: v2 Release

on:
  push:
    tags:
      - "v2.*"

permissions:
  contents: write

jobs:
  release:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Test
        run: dotnet test tests/SinochkuGames.Tests/SinochkuGames.Tests.csproj -c Release

      - name: Publish
        run: >
          dotnet publish src/SinochkuGames/SinochkuGames.csproj
          -c Release
          -r win-x64
          --self-contained true
          -p:PublishSingleFile=true
          -o dist/v2

      - name: Generate SHA-256
        shell: powershell
        run: |
          $hash = (Get-FileHash dist/v2/SinochkuGames.exe -Algorithm SHA256).Hash.ToLowerInvariant()
          Set-Content -Path dist/v2/SinochkuGames.exe.sha256 -Value $hash -Encoding ascii

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          prerelease: ${{ contains(github.ref_name, '-') }}
          generate_release_notes: true
          files: |
            dist/v2/SinochkuGames.exe
            dist/v2/SinochkuGames.exe.sha256
